<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png"href="/assets/images/logo.png">
    <title>Flight Creator</title>
    <style>
        body {
            background-color: #02124e;
            font-family: 'Times New Roman', serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }

        .container {
            width: 850px;
            text-align: center;
        }

        h2 {
            color: #FF8A02;
            font-size: 28px;
            margin-bottom: 20px;
            font-weight: 500;
        }

        .form-card {
            background-color: #011663;
            border: 2px solid #ffffff66;
            border-radius: 25px;
            padding: 50px 40px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            column-gap: 30px;
            row-gap: 15px;
            margin-bottom: 40px;
        }

        /* Label style from the original image (Orange/Gold and small) */
        .column-label {
            color: #FF8A02;
            font-size: 14px;
            font-weight: bold;
            text-align: left;
            margin-bottom: 8px;
            font-family: Arial, sans-serif;
        }

        input, select {
            width: 100%;
            padding: 14px;
            border-radius: 6px;
            border: none;
            background-color: #ffffff;
            font-size: 15px;
            box-sizing: border-box;
            color: #333;
        }

        /* Create the specific "drop-down" arrow from the image */
        select {
            appearance: none;
            background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23333' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
            background-repeat: no-repeat;
            background-position: right 15px center;
            background-size: 14px;
        }

        .button-group {
            display: flex;
            justify-content: center;
            gap: 25px;
        }

        .btn {
            padding: 12px 40px;
            border-radius: 5px;
            border: none;
            font-weight: bold;
            font-size: 15px;
            cursor: pointer;
            font-family: Arial, sans-serif;
        }

        .btn-cancel {
            background-color: #ffffff;
            color: #333;
        }

        .btn-create {
            background-color: #FF8A02; /* Original button color */
            color: #333;
        }

        #errors{
            margin-top:20px;
            color:red;
            background-color: rgba(255, 0, 0, 0.356);
            border-radius:5%;
        }
        #errors ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        #success {
            margin-top:20px;
            color:greenyellow;
            background-color: rgba(172, 255, 47, 0.349);
            border-radius:5%;
        }   
    </style>
</head>
<body>
    <%- include("partials/navbar.ejs") %>
    <div class="container">
        <h2>Create New Flight Record</h2>
        <div class="form-card">
            <form action="/admin/create-flight" method="POST">
                <div class="form-grid">
                    
                    <div class="input-item">
                        <div class="column-label"> </div>
                        <input type="text" placeholder="Flight Number" name="flight_number">
                    </div>
                    <div class="input-item">
                        <div class="column-label"> </div>
                        <input type="text" placeholder="Aircraft ID" name="aircraft_id">
                    </div>

                    <input type="text" placeholder="Departure Airport" name="departure_airport">
                    <input type="text" placeholder="Arrival Airport" name="arrival_airport">

                    <input type="text" placeholder="Date" name="date">
                    <input type="text" placeholder="Gate" name="gate">

                    <input type="text" placeholder="Boarding Time" name="boarding_time">
                    <input type="text" placeholder="Departure Time" name="departure_time">
                    
                    <input type="text" placeholder="Arrival Time" name="arrival_time">
                    <select name="status">
                        <option>Flight Status</option>
                        <option>Scheduled</option>
                        <option>Boarding</option>
                        <option>Enroute</option>
                        <option>Landed</option>
                        <option>Deboarding</option>
                    </select>

                </div>

                <div class="button-group">
                    <button type="button" class="btn btn-cancel" onclick="window.location.href='/admin'">Cancel</button>
                    <button type="submit" class="btn btn-create">Create Flight</button>
                </div>
            </form>
            <% if(locals.success){ %>
                <div id="success">
                    <p>the flight was created successfully!</p>
                </div>
            <% } %>
            <% if(locals.errors) {%>
            <% if (errors && errors.length > 0) { %>
                <div id="errors">
                    <p>The following need fixing:</p>
                    <ul>
                        <% errors.forEach(error => { %>
                        <li><%= error %></li>
                        <% }) %>
                    </ul>
                </div>
            <% }} %>
        </div>
    </div>

</body>
</html>